# x365 修改日志

## V21.29 (2026-09-03)

### 目标页面10项重大改进
1. **去掉"主目标"文字**：目标弹窗标题中去除"主目标"字样
2. **字段排序优化**：主目标名称 → 子目标（选项）→ 目标数量
3. **子目标改为选项式**：默认隐藏子目标区域，点击展开；填写子目标后目标数量自动隐藏
4. **搜索关键词关联**：主目标和子目标输入框均支持 datalist 搜索动作关键词
5. **子目标输入框优化**：支持直接输入文字，保留右侧上下箭头（▲▼）调整数量
6. **周期选择器增强**：按周定期显示"一二三四五六日"圆圈多选控件；按月定期显示 1-31 数字选择器
7. **保存反馈**：保存后关闭弹窗并显示"保存成功"提示
8. **点击卡片可编辑**：目标卡片点击后弹出编辑弹窗；删除改为左滑出现删除按钮（参考时间线交互）
9. **目标进度自动计算**：从目标设定当天起，匹配子目标动作自动计入进度，显示"当前/目标"比例，进度条用对应类别颜色填充
10. **弹窗外点击退出**：弹窗内容为空时点击遮罩区域可退出

### 交付信息
- 工作区文件：`x365-V21.29.html`（514683 字节 / ~11315 行）
- Git 仓库：`C:\Users\Administrator\x365\`
- GitHub 远程：`https://github.com/1174641046-lab/x365`
- 线上地址：`https://1174641046-lab.github.io/x365/`
- index.html 已指向 `x365-V21.29.html`

---

### 1. 统计页数据一致性修复
- 修复饼图显示次数与上方统计数据不一致问题（饼图28次，上方显示30次）
- `renderStats` 中 total 统计排除体重相关动作，与饼图/明细计算逻辑保持一致
- 周/月视图上下数据同步修复

### 2. 记录页面
- 恢复左上角用户头像按钮（V21.27删除的）
- "Persist"文字大小从12px缩到10px，font-weight加粗为500
- 氛围文字（`.atm-text`）font-weight从500加粗为600
- 标题栏 min-height 从52px改为66px（仅记录页）

### 3. 待办完成弹鼓励话 + 完成提醒变记录
- 今天待办卡片点击 ▢ 打勾后，弹出鼓励话弹窗（调用 showEncouragement）
- 完成的提醒自动转为一条记录添加到 RECORDS 中
- 刷新时间线、打卡页和统计页

### 4. 提醒系统
- 提醒添加弹窗的主体选择下拉框增加所有自定义主体（STATE.subjects + STATE.customSubjects）

### 5. 打卡页提醒短卡片格式调整
- 去掉每个卡片前面的提醒icon
- 只在该行最左边加一个统一的 ⏰ icon
- 白色胶囊格式，显示主体信息

### 6. 打卡页日历格子两行icon间隙
- `.cal-day-dots` 改为 flex-direction:column + gap:3px
- 两行icon（记录icon + 提醒icon）增加间隙，不再紧贴

### 7. 目标弹窗大改造
- 去掉"主目标"文字标题
- "目标名称"改为主目标名称
- 表单排序：主目标名称 → 子目标（选项）→ 目标数量
- 子目标为选项，勾选后展开；选了子目标后目标数量自动隐藏
- 主目标名称和子目标输入框增加 datalist 搜索关键词关联
- 子目标保留右边上下箭头（▲▼），输入框可输入文字
- 按周定期时弹出"一二三四五六日"圆圈供选择
- 按月定期时弹出日期"1-31"选择
- 点击保存后关闭窗口并显示保存成功
- 点击目标卡片可再次编辑（editGoal 函数完整实现）
- 删除方式改为左滑删除，取消右上角 X
- 从目标设定当天开始计算进度（getGoalDone 加入 createdAt 过滤）
- 进度条使用对应类别颜色显示
- 弹窗内容为空时点击弹窗外区域可退出

### 8. 饼图增强
- 中心"x次"文字从24px/700缩小为18px/500
- 增加各占比的 SVG 线条和详情标签（如"运动 17次"）
- 线条和标签使用对应类别颜色

### 9. 统计页今天按钮文字切换
- 每日视图显示"今天"，每周视图显示"本周"，每月视图显示"本月"

### 10. 打卡页返回时默认回打卡视图
- switchPage('calendar') 后默认调用 switchCalTab('calendar') 回到打卡视图

### 交付信息
- 工作区文件：`x365-V21.28.html`（514228 字节 / 11316 行）
- Git 仓库：`C:\Users\Administrator\x365\`
- GitHub 远程：`https://github.com/1174641046-lab/x365`
- 线上地址：`https://1174641046-lab.github.io/x365/`
- index.html 已指向 `x365-V21.28.html`

---

## V21.27 (2026-09-03)

### 1. 记录页面
- 删除右上角用户头像，只保留三点按钮
- 三点菜单点击后弹出"记忆库"和"提醒管理"两个选项
- 氛围卡片下方统计数据改为靠左对齐，与文字左对齐
- "Persist"字号从15px调整为12px
- 氛围文字字号增大2px
- 主体日记本恢复一行一列布局
- 今天待办卡片的提醒icon从 ⏰ 改为 ▢（方框），用户完成后打勾 ✓，弹出鼓励话弹窗

### 2. 提醒系统
- 事件名称datalist关键词关联修复，确保正确显示
- 添加提醒弹窗的日期字段改为一行两列的日期/时间（参考手动添加弹窗的日期代码，使用 datetime-local）
- 提醒系统的主体默认设为动作已有的主体（如 Bonnie 小猪）

### 3. 打卡页面
- 格子里的提醒icon到到期当天时透明度变为100%
- 同一个格子的多个提醒icon合并显示为 ⏰x3 格式，避免多个 ⏰ 拥挤
- 短卡片多列排列格式：开头一个 ⏰ icon 后跟纯文字胶囊（白天模式下白色胶囊）
  - 同时显示主体信息，如"拖地"下方显示主体"Bonnie"

### 4. 时间线
- 右滑复制/提醒按钮进一步缩小

### 5. 记录页标题栏
- 统一标题栏高度与其他页面一致（52px）

### 6. 设置页面
- 进入设置页时自动回到顶部
- 记忆库网格移动到数据管理上方
- 打卡页面进入时默认显示打卡页面

### 7. 目标页面设计
- 添加逻辑：
  1. 主目标（第一层级）
  2. 可选增加子目标（子目标=系统现有动作），子目标和子目标目标数量一行两列
  3. 第一层级目标数量
  4. 周期选择：按周定期/月定期，点击圆圈选中
  5. 第一层级预计达成日期
- 目标添加弹窗交互：输入框为空时点击弹窗外区域可退出

### 8. 统计页面
- 饼图放大一倍，居中展示
- 取消饼图右边的主体/次数/百分比列表
- 饼图改为按类别显示占比
- 切换"按类别查看⇄"按钮时，按钮文字同步切换为"按动作查看⇄"/"按类别查看⇄"

### 交付信息
- 工作区文件：`x365-V21.27.html`（530421 字节 / 10967 行）
- Git 仓库：`C:\Users\Administrator\x365\`
- GitHub 远程：`https://github.com/1174641046-lab/x365`
- 线上地址：`https://1174641046-lab.github.io/x365/`
- index.html 已指向 `x365-V21.27.html`

---

## V21.26 (2026-08-26)

### 18项修改需求

1. 统计页"按主体汇总"改"日复一日的力量"+胶囊筛选器+饼图(周/月)+取消顶部主体筛选器
2. 记录页今日待办卡片(当天到期提醒)+主体日记本一行两列+提醒识别机制("今天提醒我xx"等)
3. 提醒管理分主体+打卡页提醒短卡片多列+提醒事件datalist关键词关联+提醒内容非必填
4. 目标弹窗动作改为输入框+datalist+弹窗外点击不退出+加号icon右下角
5. 提醒弹窗日期改datetime-local
6. 通知文案改"Hi，user名称"
7. 提醒识别机制扩展
8. 记录页移除"本地"按钮+打卡页增加三点菜单
9. 设置页进入默认刷新
10. 统计页"今天"按钮缩小
11. 复合时间单位换算修复("1小时30分钟"→90分钟)
12. 时间线右滑复制/提醒按钮分两行
13. 打卡卡片圆角20px
14. 日历外框宽度与搜索框一致
15. 第x天基于当年日期计算
16. 所有页面标题栏统一高度52px
17. 氛围区改造(字号增大/去双引号/文案轮换/统计数据宽度50%/去掉发丝线)
18. 白天模式时间线点击无选中效果

### 后续追加
- 统计页标题改为"统计"
- 饼图和"日复一日的力量"明细合并为同一个 stat-card
- 胶囊筛选器移到卡片标题行右侧
- 饼图下方增加主体选择器
- statViewMode 改为 subject/category 切换，按钮文字"按类别查看⇄"/"按主体查看⇄"
